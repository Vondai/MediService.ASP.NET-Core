@using MediService.ASP.NET_Core.Services.Specialists
@using MediService.ASP.NET_Core.Infrastructure

@model IEnumerable<AppointmentViewModel>

@inject ISpecialistService Specialists
@{
    ViewData["Title"] = "My appointments";
    var isSpecialist = Specialists.IsSpecialist(User.Id());
}

<div class="container">
    <h2 class="heading-margin text-center text-warning font-weight-bolder">My appointments</h2>
    @if (!Model.Any())
    {
        <h2 class="text-center text-primary">You do not have any active appointments.</h2>
    }
    else
    {
        <table class="table table-primary table-bordered col-sm-12 offset-lg-2 col-lg-8 offset-xl-2 col-xl-8">
            <thead>
                <tr>
                    <th scope="col">Date</th>
                    <th scope="col">Time</th>
                    <th scope="col">Service</th>
                    @if (isSpecialist)
                    {
                        <th scope="col">Patient name</th>
                    }
                    else
                    {
                        <th scope="col">Specialist name</th>
                    }
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var appointment in Model)
                {
                    <tr>
                        <td>@appointment.Date</td>
                        <td>@appointment.Time</td>
                        <td>@appointment.ServiceName</td>
                        <td>@appointment.Name</td>
                        @if (isSpecialist)
                        {
                            <td><a class="btn btn-warning" href="/Appointments/Details?id=@appointment.Id">View more</a></td>
                        }
                        else
                        {
                            @if (appointment.CanCancel)
                            {
                                <td><a class="btn btn-danger" href="/Appointments/Cancel?id=@appointment.Id">Cancel Appointment</a></td>
                            }
                        }
                    </tr>
                }
            </tbody>
        </table>
    }
    <p class="font-weight-bolder">Note: <span class="text-warning">Appointments due in an hour can not be canceled.</span></p>
    <a class="btn btn-warning" href="/Appointments/Archive">My past appointments</a>
</div>

